import 'package:basic_app/root_component.dart';
import "package:flutter/material.dart";

void main() {
  runApp(
    const MaterialApp(
      home: Scaffold(
        body: RootComponent(),
      ),
    ),
  );
}

// creating a class that is the root component
